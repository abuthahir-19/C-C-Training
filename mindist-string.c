#include <stdio.h>
#include <stdlib.h>

void splitAndPerform (char str[]) {
    char newString[1001][1001];
    int chars, len = 0;
    for (int i = 0; str[i] != '\0'; i++) {
        
    }
}

int main ()
{
    char str[1001];
    scanf ("%[^\n]%*c", str);
    splitAndPerform (str);
    return 0;
}